//declaring functions
// parameters
// scope

// declaring ("creating") a function:
function display(name, age) {
  // here name is called a parameter (to the function), side note: it is more elegant to have as few parameters in a function as poss
  console.log(name, age);
}

// calling a function:
display("sven", 49);
// this is an argument
display("jan");
